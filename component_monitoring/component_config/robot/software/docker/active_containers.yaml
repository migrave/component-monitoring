name: docker_container_monitor
description: Active Docker containers
mappings:
    - mapping:
        inputs: ["overpass:latest"]
        outputs:
            - output:
                name: overpassAPI
                type: bool
